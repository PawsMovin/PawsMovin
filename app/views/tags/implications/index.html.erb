<%= render "tag_relationships/search", url: tag_implications_path %>
<%= render "tag_relationships/listing", tag_relations: @tag_implications %>
<% if CurrentUser.is_admin? %>
  <%= render "tag_relationships/add_form", type: :tag_implication, path: new_tag_implication_path %>
<% end %>

<%= numbered_paginator(@tag_implications) %>

<%= render "secondary_links" %>

<% content_for(:page_title) do %>
  Tag Implications
<% end %>
