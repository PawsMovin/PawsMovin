<% @tag = @ticket.model %>
<h4>You are reporting the following tag:</h4>
<div class="page"><%= link_to(@tag.name, @tag) %></div>
<span class="date">Created <%= time_ago_in_words_tagged(@tag.created_at) %></span>
