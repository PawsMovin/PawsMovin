<%= error_messages_for("forum_post") %>

<%= custom_form_for(forum_post) do |f| %>
  <%= f.input :body, as: :dtext, limit: PawsMovin.config.forum_post_max_size %>

  <%= f.button :submit, "Submit" %>
<% end %>
